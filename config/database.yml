development:
  adapter: postgresql
  database: librarycat_dev
  encoding: utf8
  template: template0
  host: localhost

test:
    adapter: postgresql
    database: librarycat_test
    encoding: utf8
    template: template0
    host: localhost
